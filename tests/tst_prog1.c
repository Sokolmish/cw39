//extern int printf(const char *fmt, ...);
//extern int atoi(const char *str);

int add(int a, int b) {
    return 1 + 2;
}

int main(int argc, char **argv) {
    int x;
    long y;
    add(x, 2);
//    x = 5;
//    y + 72L;
//    (long)x + y;
    while (x) {
        x = x - 1;
        if (x) {
            break;
            3;
        }
        else
            continue;
//            x;
        1 + 2;
    }
    x = x + 10;
    return x;
}
