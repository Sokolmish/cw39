//extern int printf(const char *fmt, ...);
//extern int atoi(const char *str);

int main(int argc, char **argv) {
    int x;
    x = 5;
    while (x) {
        x = x - 1;
        if (x) {
            break;
            3;
        }
        else
            continue;
//            x;
        1 + 2;
    }
    x = x + 10;
    return x;
}
